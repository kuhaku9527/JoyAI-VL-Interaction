[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "memory-store"
version = "0.1.0"
description = "JoyAI memory-store v0.1 skeleton (SQLite FTS5 + pluggable backends)"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "pydantic>=2.5",
    "httpx>=0.27",
    "numpy>=1.26",
    "usearch>=2.17",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]
local-embed = [
    # Offline bulk ingest path (ADR-0012): local bge-m3 weights.
    "sentence-transformers>=2.7",
]

[project.scripts]
memory-store = "memory_store.app:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
