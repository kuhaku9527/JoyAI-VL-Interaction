# 资源 — 模型与权重

> 本文件记录 **模型根目录约定与推理引擎** 的已确定决策，覆盖 L4 `D-2026-07-13-011` ~ `D-2026-07-28-012`。
> 所有事实由主理人亲自从目录实证 + `MEMORY.md` 模型段交叉核实（2026-07-28 召回轮，不起子代理）。

---

## D-2026-07-13-011  统一模型根 `D:\AI\models`

- **事实**: 所有模型权重统一放在 `D:\AI\models`，按用途分子目录：
  - `main/` — 主 VLM GGUF（JoyAI-VL-Interaction-Preview IQ4_NL 等）
  - `sherpa-onnx/` — ASR/KWS 的 ONNX 模型（Qwen3-ASR / bt 唤醒词）
  - `bge-m3/` — 语义召回嵌入模型（Local Wiki 文本向量化）
  - `k2-wheels/` — 相关 Python wheel 缓存
  - （如存在 `summary/` 亦归位于此根，summary 模型走 vLLM/llama.cpp）
- **来源**: git `d75faf6`（2026-07-13 整体快照，下限）+ `run-windows.ps1` 模型根引用 + 目录实证
- **校验**: `ls -d /d/AI/models/*/` → 实测含 `bge-m3/ k2-wheels/ main/ sherpa-onnx/`
- **预期**: 模型不散落 `services/` 或 `D:/tmp` 等路径，统一在 `D:\AI\models`
- **Drift**: 无（注：kws-training 脚本内数据路径 `/mnt/d/AI/data/kws/...` 是训练期外溢，非运行时，见 `服务-语音栈.md` D-046）
- **Owner**: DevOps / ML
- **锁定**: 🔒

---

## D-2026-07-28-012  VLM 推理引擎：llama.cpp `b10155` win-cuda-13.3 替换史

- **事实**: VLM(:7060) 推理引擎经替换：原 `D:\AI\bin\llama.cpp`（CUDA13）在 RTX 5060 Ti（Blackwell）上 `0xc0000374` 堆损坏崩于 slots 初始化；备份 `andgihat-9150-backup`（CUDA12）SIGILL 崩于 context 构造。**现稳定版 = `b10155`（CUDA 13.3）**，监听 :7060。两旧版保留作回滚。
  - 补充：vLLM 是主 VLM + 总结模型的**默认生产路径**（见 `服务-VLM.md` D-020）；llama.cpp b10155 是经替换验证的稳定引擎候选，非默认生产必需。
- **来源**: `MEMORY.md` 模型框架段（2026-07-28 实测替换并验证稳定）
- **校验**: `ls -d /d/AI/bin/llama.cpp/` → 存在 b10155 稳定版；`logs/llama-main.log` 应见 b10155 启动并监听 :7060（注意当前 n_ctx 回退见 `服务-VLM.md` D-022 / `drift-历史.md`）
- **预期**: 活动 llama-server 来自 b10155（CUDA13.3）；旧两版保留未删
- **Drift**: 运行态 n_ctx 回退 4096（见 D-022，决策态 16384）——属启动路径问题，非引擎替换问题
- **Owner**: DevOps / ML
- **锁定**: 🔒

---

## 关联索引

- VLM 服务真值（端口/ctx/运行态）：见 `服务-VLM.md`（D-020/021/022）
- ASR/KWS/TTS 模型：见 `服务-语音栈.md`（D-045/046/047）
- 语义召回 bge-m3：见 `服务-memory-store.md`、`业务-LocalWiki.md`
- 隔离纪律（禁外溢 D:/tmp）：见 `跨域铁律.md`（D-L4-002）
