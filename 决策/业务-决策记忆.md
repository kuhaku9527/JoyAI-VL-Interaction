# 业务域 — 决策记忆 / 长期记忆

> 本文件记录 **决策记忆与长期记忆** 的已确定决策，覆盖 L3 `D-2026-07-13-075` ~ `D-2026-07-28-076`。
> 所有事实由主理人亲自从 git 提交 + 代码（`services/webinfer/memory_io.py`、`infer_loop.py`、`services/background-agent/`）核实（2026-07-28 召回轮，不起子代理）。

---

## D-2026-07-13-075  决策/长期记忆 `_memory_recall` 函数存在

- **事实**: webinfer 的 `MemoryIOMixin` 提供 `_memory_recall(state, question)` 方法——每轮对话对用户提问触发一次记忆召回（warmup 缓存优先，未就绪则临时 warmup），结果落入 `state._memory_wiki_cache` 等槽位，供 prompt 渲染。
- **来源**: git `d75faf6`（2026-07-13 整体快照，下限）
- **校验**: `grep -n "async def _memory_recall" services/webinfer/memory_io.py` → :157 定义；`grep -n "_memory_recall" services/webinfer/infer_loop.py` → :159 注释（"must be reached from the production text path"）/ :168 `await self._memory_recall(state, last_user_text)`
- **预期**: `_memory_recall` 在 memory_io.py 定义且于生产文本路径被调用
- **Drift**: 无
- **Owner**: 后端 / ML
- **锁定**: 🔒

---

## D-2026-07-28-076  聊天注入 Local Wiki 语义召回（fail-open，env 可控）

- **事实**: 每次玩家提问，经 `_memory_recall` 触发 **Local Wiki 语义召回**（memory-store :8997），结果 fail-open 注入聊天 prompt（仅 warning，不阻断）。召回行为由 env 旋钮控制：`WIKI_RECALL_ENABLED` / `WIKI_RECALL_NAMESPACES`（默认 `wiki:*`，background-agent 侧）/ `WIKI_RECALL_TOP_K` / `WIKI_RECALL_MIN_SCORE`；env 优先于配置。
- **来源**: git `c8da4de`（#42，2026-07-28，ADR-0012 §6 默认 embedder=local + 聊天注入 wiki）
- **校验**:
  1. `grep -n "WIKI_RECALL_ENABLED\|WIKI_RECALL_NAMESPACES\|WIKI_RECALL_TOP_K\|WIKI_RECALL_MIN_SCORE" services/webinfer/memory_io.py` → :41/47/53 定义读取
  2. `grep -n "WIKI_RECALL_NAMESPACES" services/background-agent/hermes_api/main.py` → :55 默认 `"wiki:*"`；:255 按逗号解析 namespaces
  3. `grep -n "_memory_recall(state, last_user_text)" services/webinfer/infer_loop.py` → :168 生产路径调用
- **预期**: 四个 WIKI_RECALL_* env 被读取；默认 namespace=`wiki:*`；生产文本路径调用 _memory_recall
- **Drift**: 无（注意 #38 默认 bge-m3 provider=nvidia 的 P1 待裁决见 `业务-LocalWiki.md` D-074，与本条目 recall 注入机制独立）
- **Owner**: 后端 / ML
- **锁定**: 🔒

---

## 关联索引

- 记忆 warmup 信号：见 `服务-webinfer.md`（D-025）
- 调用方 background-agent `_enrich_with_memory`：见 `服务-background-agent.md`（D-049）
- Local Wiki 全链路 / namespace 规则：见 `业务-LocalWiki.md`
- 向量召回后端（memory-store）：见 `服务-memory-store.md`
